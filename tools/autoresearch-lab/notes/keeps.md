# KEPT improvements (MASSE Autoresearch Lab Gen 1)

This is the archivist log. Keep it short, factual, and tied to the ledger entries.

## 2026-03-11

### Keep: solver system = BandGeneral
- Change: `solver.system` set to `BandGeneral` (from UmfPack)
- Why it matters: same outputs, slightly faster solves in this benchmark suite
- Evidence: ledger entry marked KEEP (see `results.tsv`)

### Keep: numberer = Plain
- Change: `solver.numberer` set to `Plain` (from RCM)
- Why it matters: same outputs, slightly faster solves in this benchmark suite
- Evidence: ledger entry marked KEEP (see `results.tsv`)
